# Configuration is optional. Copy to config.toml and adjust when needed:
#   cp config.example.toml config.toml

import_root = "/path/to/your/finance/documents"
db_path = "data/wr.sqlite"

# Fiscal-partner settings are optional. For an individual return, omit this
# section: the taxpayer name is read from the imported tax return.
#
# Add only the entries you need when statement holder names require aliases:
# [partners]
# partner_a = "Sophie de Vries"
# partner_a_aliases = ["S. de Vries", "Sophie M. de Vries"]
#
# For full-year fiscal partners, partner_b and partner_b_aliases can be added
# to the same section.
